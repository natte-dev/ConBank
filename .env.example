# =============================================================================
# .env.example — conBank
# Copie para .env e preencha os valores. NUNCA commite o .env.
# =============================================================================

# -- BACKEND ------------------------------------------------------------------
# Obrigatório. Configure no EasyPanel ? App (backend) ? Environment
DATABASE_URL=postgresql://conbank:TROQUE_A_SENHA@postgres:5432/conbank

# Origens permitidas para CORS — coloque a URL do frontend
# Separe múltiplas por vírgula
ALLOWED_ORIGINS=https://frontend.seuserver.easypanel.host

# -- FRONTEND (Build Args) ----------------------------------------------------
# Obrigatório. Configure no EasyPanel ? App (frontend) ? Build Args
# É a URL pública do backend (sem barra no final)
VITE_API_URL=https://backend.seuserver.easypanel.host
